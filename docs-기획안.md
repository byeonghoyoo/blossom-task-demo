# BLOSSOM TASK — 기획 요약 (Spec)

> 원본 `TEAM-TASK_기획안.md`가 이 배포 세션에 없어, README 스펙을 기준으로 재구성한 요약본입니다.
> 정식 기획안으로 교체하려면 이 파일을 원본 내용으로 덮어써 주세요.

## 개요
뷰티블라썸 마케팅팀의 **테스크 일지(업무 기록) 웹앱**. 매일의 루틴 업무 수행·기록, 원장/관리자 지시 전달, 월간 성과 추적을 한 화면에서 관리한다. 본 저장소는 **보고·컨펌용 인터랙티브 UI 목업**이며 데이터 저장 기능은 없다.

## 화면 구성
| 화면 | 핵심 요소 |
|---|---|
| **오늘**(진입 화면) | 진행률 링, 루틴 체크(완료 시 취소선), 실시간 루틴 깜빡임, 원장 지시 배지 |
| **월간 그리드** | 날짜별 완료율 바, 오늘 강조, 월 요약 KPI |
| **기록·검색** | 업무 로그 리스트, 키워드 검색, 카테고리 칩 필터(SNS/DB/광고/이벤트) |
| **관리자** | 팀 KPI, 팀원별 진행률, 전체 지시 발송 |

> **인증**: 본 데모는 개발 전 단계 시안이므로 로그인 화면 없이 바로 진입한다.
> 실제 구현 시 Supabase Auth 기반 팀 계정 로그인을 앞단에 추가할 예정.

## 인터랙션 규칙
- 루틴 항목 탭 → 완료/해제 토글, 진행률 링 실시간 갱신
- 하단 탭바로 4개 주요 화면 이동
- 기록 화면: 검색어 + 칩 필터 동시 적용

## 구현 스택 (예정)
- **Frontend**: Next.js 15 + Tailwind CSS v4
- **Backend/DB**: Supabase (Auth · Postgres · Realtime)
- **배포**: Vercel
- 현재 목업은 **단일 HTML 파일**(의존성 없음)로 GitHub Pages에 정적 배포.

## 비고
- 목업 데이터는 예시이며 실제 팀원/실적과 무관하다.
- 색상: 브랜드 핑크(#c65b8a) → 퍼플(#8a5bc6) 그라데이션.
